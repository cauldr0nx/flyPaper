# The other half of the circuit

Everything measured in this project so far concerns the projection - how receptor channels reach the Kenyon cells. This is the readout: who reads those cells, and how hard. The published Fly Bloom Filter assumes MBON-alpha'3 reads every Kenyon cell equally, exactly as it assumed a uniform fan-in and a random projection.

**The measurement disagrees more sharply here than anywhere else.** Of 2,045 Kenyon cells, **345 synapse onto MBON-alpha'3 and 1,700 do not.** That is expected anatomy rather than a surprise - the alpha'3 compartment is innervated by alpha'/beta' Kenyon cells and our cell set is the whole hemisphere - but it means the published filter and the measured one are not small variations on each other. One reads 2,045 cells and the other reads a 6th of that.

Those 345 cells form a complete circuit of their own: 53 of the 55 glomeruli reach them, at a mean fan-in of 4.2. So the faithful model is not a mask over a big tag - it is a smaller mushroom body, and it is tested here as one.

## Where the hardest labelled response lands

Offline, `v4-glomerular`, median over 8 seeds with [min-max]; the measured wiring is a single circuit and has no seed. Lower is better.

| Surface | `uniform-2045` published | `masked-2045` naive | `random-345` size-matched | `measured-345` faithful |
|---|---:|---:|---:|---:|
| `bench-sprawl` | 934 [18-1656] | 1326 [16-1976] | 1334 [550-1524] | **956** |
| `bench-mixed` | 24 [6-906] | 738 [311-916] | 504 [6-708] | **248** |
| `bench-token` | 39 [8-592] | 1265 [8-1998] | 1348 [536-1998] | **1997** |
| `bench-collide` | 6 [6-6] | 6 [6-717] | 6 [6-99] | **6** |
| `bench-stable` | 6 [6-6] | 6 [6-6] | 6 [6-6] | **6** |

## Interpretation

**Reading fewer cells costs a great deal, and that is the whole result.** `random-345` is the published filter with nothing changed but the number of cells the readout can see, and it is worse on every surface that discriminates at all. A tag is 5% of the layer, so a 345-cell layer carries about 17 active cells against 102 - and a filter's capacity is how many distinct things those cells can keep apart.

**The naive reading is worse still, and deserved to be.** `masked-2045` keeps the full tag and lets only the connected cells drive the readout, which is not what the fly does; it is five sixths of a code computed and discarded. It is reported because it is the first thing anyone would try.

**Within its own size the measured wiring is not clearly better than random.** Compared against `random-345`, the faithful circuit wins on some surfaces and loses on others, which is the same answer the projection comparison gave and is consistent with there being nothing in this workload that the fly's particular wiring is for.

**What this says about the model, rather than about the tool.** The published 2,045-cell filter is not a neutral simplification of the anatomy - it is doing real work, and the work it is doing is capacity. A real MBON-alpha'3 grades novelty over a few hundred odours encountered across a fly's life. A content discovery scan asks the same circuit to keep two thousand distinct response shapes apart in fifty seconds. `reports/decay.md` found this tool's capacity limit at around 200 distinct shapes; this is the anatomical reason for a limit of that order, arrived at independently.

### What would change this

- Kenyon cell subtypes. Our cell set is every Kenyon cell on the hemisphere, and alpha'3 reads only the alpha'/beta' ones. If the 345 were resolved by subtype annotation rather than inferred from who happens to synapse onto this MBON, the restricted circuit would be a claim about anatomy rather than a reasonable guess at it.
- The other MBONs. alpha'3 is the novelty readout in the papers, and it is one compartment of many. A filter reading several compartments would have more capacity and would no longer be the published model.
- Reconstruction completeness. A cell with no recorded synapse onto alpha'3 in this volume is not proof of a cell with no synapse onto alpha'3.

---

*Generated by flypaper 0.1.0 at commit `483625ebb0ba9abeed9c9cd33ba1fc641346d1e4` **(dirty working tree - not reproducible)** | seed `0` on 2026-09-13T06:26:34+00:00.*
